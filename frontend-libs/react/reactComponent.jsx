class MyComponent extends React.Component {
  {/* Constructor function makes sure that the properties of the base react component class are included in this new component */}
  constructor(props) {
    super(props);
  }
  render() {
    return (
      <div><h1>Hello React!</h1></div>
    );
  }
};
