// In Redux, all state updates are triggered through actions
// Actions are js objects that contain info about an event that occurred
// Actions must include a type property that indicates the type of action that occurred
const action = {
    type: "LOGIN"
};