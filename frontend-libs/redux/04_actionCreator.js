const action = {
    type: 'LOGIN'
}

// after creating action, it should be sent to the Redux store to update state.
// an action creator is a function that returns an action

function actionCreator() {
    return action;
}