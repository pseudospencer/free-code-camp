function convertToRoman(num) {
    let numerals = {
        1   : "I",
        5   : "V",
        10  : "X",
        50  : "L",
        100 : "C",
        500  : "D",
        1000  : "M"
    }

    // get number of digits
    let numStr = num.toStr();
    let places = numStr.length;

}
